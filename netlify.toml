[build]
  # Directory with the build output
  publish = "dist"
  
  # Build command
  command = "npm run build"

# Redirect rules for SPA (if needed)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
